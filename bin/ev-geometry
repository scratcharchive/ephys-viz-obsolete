#!/usr/bin/env node

const electron=require('electron')
 
args=[`${__dirname}/../electron/openpage.js`,'index.html','--view=geometry'];
args=args.concat(process.argv.slice(2));

require('child_process').spawn(electron,args);
